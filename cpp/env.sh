#!/bin/zsh

# Export current directory to PIXELS_SRC and PIXELS_HOME
export PIXELS_SRC=$(pwd)
export PIXELS_HOME=$(pwd)

# Optionally print the values to verify
echo "PIXELS_SRC is set to $PIXELS_SRC"
echo "PIXELS_HOME is set to $PIXELS_HOME"

